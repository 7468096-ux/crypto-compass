import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "CryptoCompass - Navigate the Crypto Market",
  description: "Track top cryptocurrencies, build portfolios, and simulate investments with real-time data.",
  keywords: ["cryptocurrency", "bitcoin", "ethereum", "crypto tracker", "portfolio"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-gray-950 text-white antialiased`}>
        {children}
      </body>
    </html>
  );
}
